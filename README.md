# Chat-with-data
Agent assist user in getting information from the database. Datasource should not go to LLM fully (in prompt), only part of it may pass through, e.g. extracted chunks or output from the DB.
